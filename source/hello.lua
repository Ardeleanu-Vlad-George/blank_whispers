#!/usr/bin/lua
-- Above 'shebang' doesn't point to an installed utility.
-- But it's needed because the 'blank_whispers' tool
-- expects it.
local msg = "Hello, People!" --? Consider changing the message
print(msg)
